# main github repo used for hosting a wordpress blog

# steps to manage the blog, now using jekyll
git checkout source

gem install jekyll
gem install hamilton # may need to edit *.scss to use 'calc(x / y)'

jekyll build # builds the static content under _sites
jekyll serve # builds and serves up static content

# deploy to github
deploy the _sites/* contents to the main branch

# for embedding in github pages see: https://github.com/nathancy/jekyll-embed-video?tab=readme-ov-file
